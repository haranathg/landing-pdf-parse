# Landing.AI ADE API Key (required for Landing AI parser)
VISION_AGENT_API_KEY=your_vision_agent_api_key_here

# Anthropic API Key (required for Claude Vision parser and chat)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Google Gemini API Key (required for Gemini Vision parser)
GOOGLE_GEMINI_API_KEY=your_gemini_api_key_here

# AWS Bedrock Configuration (for Bedrock Claude parser)
# No API key needed - uses IAM credential chain:
# 1. Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# 2. IAM role (when running on AWS)
# 3. ~/.aws/credentials file
AWS_REGION=us-east-1
# Optional: explicitly set credentials (not recommended for production)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
